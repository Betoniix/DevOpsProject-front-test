pipeline {
    agent any
    stages {
        stage('SSH Commands') {
            steps {
                script {
                    sshagent(['credenciales-ssh']) {
                        sh '''
                        ssh -tt cisco@192.168.100.42 <<'EOF'
                        echo "Conexión SSH exitosa"
                        cd Desktop/
                        git clone https://github.com/CL-Nayib/DevOpsProject-front.git
                        cd DevOpsProject-front || exit 1
                        git switch develop
                        npm install -g @angular/cli
                        npm install
                        ng serve
                        EOF
                        '''
                    }
                }
            }
        }
    }
}
